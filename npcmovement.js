function NPCMovement() {
  return {
    move: function(start, end) {
    },
    path: function(start, end) {
    },
    // TODO
    // - allow movement cancel
    // - movement finish callback
    // - check pathfinding at every frame? because what if something moves
    //   in the way? or only on collision maybe?
  }
}
